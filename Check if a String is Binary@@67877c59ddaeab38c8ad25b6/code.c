#include <stdio.h>

int main() {
    char name[20];
    scanf("%s",name);
    for(int i=0;i<n;i++){
        char c = name[i]
        if(c=='1' && c=='0'){
            printf("Yes");
        }
        else{
            printf("No");
        }
    }
    return 0;
}