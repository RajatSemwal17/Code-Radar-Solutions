#include <stdio.h>

int strlen(char name[]){
    
}

int main() {
    char name[30];
    // fgets(name,sizeof(name),stdin);
    scanf("%s",strlen(name));
    printf("%d",strlen(name));
    return 0;
}