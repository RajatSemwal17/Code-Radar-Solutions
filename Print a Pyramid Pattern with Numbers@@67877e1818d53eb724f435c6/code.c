#include <stdio.h>

int main() {
    int n;
    scanf("%d",&n);
    for(int rows=0;rows<n;rows=0){
        int space
        for(int cols;cols<rows;cols++){
            printf("%d ",cols+1);
        }
    }
    return 0;
}